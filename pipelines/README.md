# Data Pipelines

Placeholder for data prep scripts and notebooks.

- Swap mock JSON with Databricks tables or parquet outputs
- Output schema: regions.json and projects.json formats
- Geo: produce countries.geojson with `region_id` in feature properties
